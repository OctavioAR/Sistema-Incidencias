# Dependencias que se necesita instalar en ambas carpetas:
npm install

# Instrucción para iniciar la API y la Página:
npm run dev

# Instalar XAMPP para la BD y ejecutar el script de SQL:
BD incidencias.txt
